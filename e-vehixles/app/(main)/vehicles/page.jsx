// "use client" is NOT needed here. 
// Only put "use client" in components that use hooks.

import VehicleMarketplace from "./VehicleMarketplace";

export default function VehiclesPage() {
  return <VehicleMarketplace />;
}
